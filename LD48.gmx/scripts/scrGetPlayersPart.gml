var result = noone

with(objLevelPart)
{
    if (argument0 >= x1 and argument0 < x2 and argument1 >= y1 and argument1 < y2 and !dragging and !animation_lock)
    {
        result = self
    }
}

return result
